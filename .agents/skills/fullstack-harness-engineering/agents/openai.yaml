interface:
  display_name: "Full-Stack Harness Engineering"
  short_description: "Plan, verify, and land delivery through reviewed PRs"
  default_prompt: "Use $fullstack-harness-engineering to plan traceable delivery, preflight permission boundaries, run verified missions, land through current-head CI and GitHub review, use gated auto-merge when authorized, and safely close merged local branches and worktrees under separate cleanup authorization."
policy:
  allow_implicit_invocation: true
