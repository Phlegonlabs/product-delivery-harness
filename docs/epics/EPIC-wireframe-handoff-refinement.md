# Wireframe Handoff Refinement

Status: release preparation authorized; exact-candidate verification pending. Route: direct, one writer and one verification sequence.
Baseline: remote main 59ae7203d6fb2c3848d3b058697dd06c4b581ed1; canonical seven-skill bundle. This source repository has no product PRD; the skills and four READMEs are the current contracts.

Problem: design translation, repeated content/interaction work and skill-update impact leave too much interpretation between PRD, wireframe and HiFi.
Accepted outcome: explicit incremental/full-rebuild modes; translation checkpoint; geometry/control/motion and bilingual reading; four Web and three iPhone recipes; source-aware freshness inspection. iPad only when product scope requires it. Preserve approved product requirements, historical artifacts, human approvals and separate native proof.

Write scope: canonical UI skill sources/tests, applicable Product Definition and Harness routing, four README descriptions and this task record. UI impact: both for the reviewer prototype; no production product surfaces or stack change. Template changes require checker/runtime regression.
Authorization: owner approved the discussed scope and explicitly allowed a new worktree/branch. Worktree: product-delivery-harness-wireframe-handoff-refinement; branch: wireframe-handoff-refinement. No commit, push, installation, delegation, archive, deletion or cleanup grants.
Gitignore: no new artifact class; source recipes/tests/docs tracked, existing __pycache__/ rule handles Python output. Verification logs stay outside the checkout.

Dependencies: use current remote canonical composition/search runtime; loaded installed-skill digest is unknown and not equated with disk. No installation or production design approval is claimed.
Checks: all repository-required spec/lint/document-weight/unit/golden-path suites, focused renderer/contract/freshness/recipe regression, git diff --check and ignore hygiene. No formal security PASS without a fixed committed candidate. No real iPhone/native proof from HTML.
Result: accepted local source changes implemented and checked. No release, installation, native-device verification or fixed-SHA security approval is claimed.

## Implementation Evidence

- Added translation/full-rebuild, reading/iPhone, reference-card and freshness guidance; updated UI, Product Definition and Harness entry points plus all four README descriptions.
- Added seven data recipes using the existing renderer, not seven copied shells. Added paired language copy with independent approval/source records, action appearance controls, compact inline navigation and switchable motion annotations. Existing generic overlays remain the route for modal drawers.
- Added read-only freshness inspection for inventoried file/input hashes, loaded/installed skill identity and transitive dependencies. It never certifies complete inventory or approval and never auto-regenerates designs.
- Focused checks: 82 existing wireframe tests passed before the final bounded additions; 15 new refinement tests pass, including closed-menu QA and iPhone-only targets. Final suite results are recorded below.
- Browser repair family: closed details content initially caused a false boundary failure. The first repair skips only hidden disclosure content, reruns QA on toggle and has a negative test proving open overflowing content still fails. No threshold was weakened.
- Isolated Chromium session checked 390/1200 Web targets, click/Enter/Escape, restored summary focus, responsive expanded navigation, bilingual copy inventory, annotation visibility, and 375/430 named iPhone projections. Observed QA passed and browser errors were empty. These are synthetic local renderer checks, not human approval or native-device evidence.
- Browser fixtures/captures: `C:/Users/mps19/AppData/Local/Temp/wireframe-refinement-preview-6c_v3esi/`. Full suite logs: `C:/Users/mps19/AppData/Local/Temp/wireframe-handoff-checks-fg__66iz/`.
- Ignore hygiene: existing .env and __pycache__ rules apply; new source recipes/tests/docs are not ignored. No new ignore entry or untracking is needed.
- Release/version promotion and installed-skill updates remain outside this local uncommitted change. The original checkout's untracked scripts directory and other worktrees are preserved.

## Final Verification

- Required dependency install, skill specification, pyflakes, docs_weight and both staged/unstaged diff whitespace checks completed successfully. Documentation weight grows by 3,223 words versus v0.48.0; the Harness core remains below its existing 3,600-word limit.
- The full Harness run executed 1,156 tests with 16 platform/opt-in skips. Its initial result was 11 failures and 2 errors: installer tests stopped on the newly untracked recipe/source files, plus two documentation contract assertions. That original failure log is retained, not rewritten as PASS.
- Repair: stage only the seven newly authored files (no commit), retain the existing incremental-precedence sentence with its explicit full-rebuild exception, and route freshness through existing references rather than expanding the core skill. The complete failed groups then passed: 60 skill-contract tests and all 15 installer tests. No installer or test assertion was weakened. No source files changed during the installer rerun.
- Other full suites passed: Product Definition 217; UI Design 204 (including all 15 new tests); Design System Compiler 98 with 3 platform skips; Product Activation 56; SEO Growth 16. The opt-in golden path passed (1 test). Final affected joins also passed: strict authority 16, cross-skill pipeline 14 and hybrid publication 2.
- Browser checks are synthetic Chromium evidence: bilingual language blocks and complete copy inventory, motion annotation visibility, compact menu click/Enter/Escape, focus restoration, open-state retention through annotation redraw, desktop expansion and declared phone canvas widths. The freshness CLI also correctly returns review_required for unknown loaded identity without rewriting files.
- Main remains unchanged with its original untracked scripts directory. New files are staged because installer validation requires a tracked source inventory; other edits remain unstaged. No commit, push, tag, installation, cleanup or formal release action occurred.

Remaining separately authorized work: fixed-candidate review and release preparation (including the version bump if promoting), publication/promotion, and installer transaction only when requested. These are not part of this local work authorization. Native iPhone behavior is validated in a target product's implementation stage.

## Release Preparation — 2026-09-21

Owner instruction: "commit and push merge to main" authorizes this scoped commit and branch publication. The repository also requires a separate main-promotion grant naming the exact candidate SHA; obtain it after verification. Release 0.49.0 includes the required version pins and four-language history. Mandatory installer update follows publication; preserve backups and all worktrees. No cleanup is authorized. Earlier local-only limitations above describe the prior handoff. Remote main was rechecked at the recorded base. Exact candidate and landing results are retained in external verification logs and the PR to avoid changing the reviewed tree.

Review repair: GitHub blocked PR 116 on unresolved review threads. Strict schema-4 region checks now require motionSpec for motion treatments; non-strict inspection remains readable. All four upgrade paragraphs now target 0.49.0 and explain freshness/motion migration. Added a regression for both motion treatments and both strict flags. Exact-SHA main authorization remains pending; no admin bypass. The original candidate passed the full suite, but the repair requires fresh candidate evidence.

## Change Log — 2026-09-24 Four Wireframe Review Widths

- Request: owner selected 390, 768, 1024 and 1440 px and authorized updating the shared template, matching docs and checks on `codex/wireframe-four-widths`.
- Baseline and observed HEAD: `077cd3471d267d2c2f093c8d06f3f76a293ed1e1`, first observed on `main`, then on the authorized branch. Pre-existing edits to `docs/DOCUMENTS.md`, `EPIC-handoff-document-audit.md` and untracked `scripts/design/hifi_model.py` are preserved; their verification is outside this task.
- Scope: current schema-5 template defaults and both example screens, wireframe guide, four README descriptions, template contract regression and real-browser width/selection checks. UI impact: structure for the review template; no production UI or stack change. Schema-4 and approved product-specific responsive sets keep their existing meaning.
- Acceptance: four target controls; every example screen declares all four layouts; actual canvases render at each selected width and retain selection after reload. Existing product contracts remain authoritative, including native size classes and other approved viewport sets.
- Route and authority: direct, one writer. Local edits and the named branch are authorized. No commit, push, release, installation or cleanup is requested. No PLAN/RUN is needed.
- Gitignore: no new artifact class. Existing Python cache and node_modules rules apply; verification logs are checkout-external. Loaded skill identity remains unobserved; installed Harness is 0.54.1.
- Working-tree implementation complete; required verification pending. Source fingerprints, results and handoff audit will be appended after checks.

### Four-width verification and handoff — 2026-09-24

- Result: both schema-5 example screens have 390/768/1024/1440 controls and layouts. Chromium measured all four exact canvas widths on both screens and verified selection after reload. The legacy template has no diff. No renderer logic, checker minimum, existing product contract or native size class changed.
- Passed: dependency setup (`pip`, `npm ci`, Chromium), skill specification, pyflakes, document weight, 66 wireframe contract tests, 5 browser tests, golden path (1), Product Definition (235), UI Design (281), Design System Compiler (99 with 3 platform skips), Product Activation (56), SEO (17), ignore hygiene and `git diff --check`.
- Harness: the first invocation reached its 1,200-second deadline. Its recorded process tree was terminated and absence confirmed. A bounded continuation restarted the interrupted test and completed the remaining 481 tests successfully, with 4 skips. Across both logs, 1,225 unique Harness test IDs were observed. This is not a full-suite PASS: the first segment recorded two failing tests, reproduced separately below.
- Unresolved failure 1: `test_running_worker_without_a_worktree_requires_reconciliation` expects only the unavailable-worktree warning; the actual dirty checkout also produces `parent: live integration checkout is dirty`. The isolated rerun reproduces it. The test and implementation are unchanged by this task.
- Unresolved failure 2: `test_bash_destination_lock_serializes_installers` misses its 10-second lock deadline and then errors while cleaning its in-use temporary directory. An isolated rerun reproduces the failure and teardown error. The leaked installer PID and descendants were inspected, stopped, and confirmed absent. Installer/test source is unchanged; no shared local skill installation occurred. Test temporary files are retained where cleanup failed.
- Evidence: [check results](C:/Users/mps19/AppData/Local/Temp/pdh-four-widths-umxg5rhz/results.json), [initial Harness log](C:/Users/mps19/AppData/Local/Temp/pdh-four-widths-umxg5rhz/harness.log), [continuation log](C:/Users/mps19/AppData/Local/Temp/pdh-four-widths-umxg5rhz/harness-continuation.log), [reconciliation rerun](C:/Users/mps19/AppData/Local/Temp/pdh-four-widths-umxg5rhz/reconciliation-recheck.log), [installer rerun](C:/Users/mps19/AppData/Local/Temp/pdh-four-widths-umxg5rhz/installer-recheck.stderr.log). The [scoped source inventory](C:/Users/mps19/AppData/Local/Temp/pdh-four-widths-umxg5rhz/source-hashes.json) has SHA-256 `557e402cf32508694fd50dde4a0b3aa2ac26665d613558d60135f3262b7a9108`; all eight source/document hashes were unchanged throughout verification. Epic/index record updates are excluded from that inventory.
- Handoff: repository `product-delivery-harness`, branch `codex/wireframe-four-widths`, HEAD `077cd3471d267d2c2f093c8d06f3f76a293ed1e1`; changes remain unstaged working-tree edits. Pre-existing documentation edits and `scripts/design/hifi_model.py` remain intact. No commit, push, release, install, branch deletion or worktree removal occurred.
- Document audit: the guide and four READMEs agree with the template-default scope and preserve exact PRD authority. The index links this record. No product PRD, architecture pair or managed PLAN/RUN is introduced. Installed Harness remains 0.54.1; observed AGENTS template SHA-256 is `86cc1595619c097f79ef8e4234367e9ba8b056a9635bc24d372bcf417b024aa6`. Applicable shared entry/change/handoff rules are current by meaning; source-repository paths and stricter release rules are retained. Document sync reports first-observation/unknown loaded identity and historical README migration/version references, not a new approval or runtime PASS.
- Next action: review the local wireframe-template diff. The unrelated Harness test failures need separate repair before claiming an entirely green release candidate. No release or installation is requested by this task.

## Branch publication authorization — 2026-09-25

The owner now requests scoped commits, push of `codex/wireframe-four-widths`, and renewal of all seven Harness skills with the official installer. This supersedes the earlier local-only next action; it does not request main promotion or a release. Verified source hashes are unchanged. Preserve unrelated audit documentation and `scripts/design/hifi_model.py`. The two recorded broader Harness test failures remain open; branch publication is not a full-suite or release PASS. Exact commit, remote readback and installation/backup evidence will be reported after execution. The observed Codex task list has no other active skill-using task; finish skill consumption before installation and start a fresh session afterward.

## HiFi layout integrity follow-up — 2026-09-25

Owner reports possible collapsed buttons/layers and asks for internal anti-slop and alignment rules. Direct documentation enhancement, UI impact none in this source repository. Baseline remains `077cd3471d267d2c2f093c8d06f3f76a293ed1e1` on `codex/wireframe-four-widths`. The interrupted publication command staged the four-width subset and appended authorization notes; no commit, push or installation occurred. Keep that index and all unrelated audit/script changes intact. Publication remains paused while these rules are reviewed.

The existing rubric already blocks overlap/clipping. Added concrete authoring and browser-inspection instructions for compressed controls, normal-flow sizing, clipping ancestors, hit testing, open layers, alignment, text stress and intermediate Web widths. Anti-slop findings must cite task/direction and inspected captures; simple designs and justified shared components are allowed. Measurements stay in existing capture evidence, with no receipt schema, dependency, score threshold or approval change. Routed HiFi authoring/repair to these rules and updated all four README descriptions. Hallmark is not bundled or required. No actual faulty product HiFi was supplied, so its diagnosis and repair remain unverified.

Acceptance: clear blocking layout criteria, reproducible inspection steps, narrow intentional-overlap exceptions, no masking defects with overflow hiding, and task-based visual judgments. Existing review budget and final matrix remain binding. No new generated artifacts or ignore rules; logs remain outside the checkout. Verification pending.

Owner follow-up requests online modern Web/iOS design and recent AI-coding research. Added `modern-design-sources.md`, routing from UI entry/platform authoring and all four READMEs. Primary sources were inspected on 2026-09-25: W3C, web.dev, Vercel, Apple WWDC, Android, Anthropic (2025-11-12 and 2026-03-24), and the living Google Labs Stitch skills repository. The guide records adoption limits: no required new stack/dependency, no generic font bans or forced glass aesthetic, no new design authority or extra repair loops. Supplemental 320 CSS-pixel reflow does not replace the four authored Web targets. Apple dynamic HIG pages were only partially extractable; official talk transcripts supply inspected evidence. Existing product packages and actual native/HiFi behavior are not certified by this research.

Before this research addition, specification, lint, document weight, UI suite, Harness skill-contract and cross-skill checks all passed; logs are in `C:/Users/mps19/AppData/Local/Temp/pdh-layout-rules-lvupr_da`. Rechecking the changed documentation follows. Previously recorded unrelated Harness failures remain open.

A second recency search found and inspected Apple WWDC26 design principles, iOS branding and agent-prototyping transcripts, plus Vercel's 2026-06-25 product-design article. Added their task clarity, native familiarity, realistic content and decision-rationale guidance; retained existing direction counts, tooling choices and approval boundaries.

### Local verification and handoff — 2026-09-25

UI suite: 281 passed, including required Chromium checks; Harness skill contract: 60 passed; cross-skill pipeline: 15 passed. Specification, pyflakes and documentation-weight checks passed. After adding the final four research sources, repeated specification, documentation weight, all 11 UI skill-contract tests and whitespace checks passed. Logs and final source hashes: `C:/Users/mps19/AppData/Local/Temp/pdh-modern-sources-5jsjpgk6`. These checks validate the rules package, not a real product's layout or native behavior. The broader Harness suite's two previously recorded failures remain unresolved and was not repeated for this documentation-only follow-up.

Checkpoint: same repository/branch and HEAD `077cd3471d267d2c2f093c8d06f3f76a293ed1e1`; working-tree documentation changes plus the earlier staged four-width subset. No commit, push, installation or cleanup occurred. The prior user pause on publication remains in effect. Unrelated audit documentation and untracked design script are preserved. The index and four README descriptions agree with source routing. No product PRD/architecture or managed PLAN/RUN applies. No new ignored artifact class is introduced.

Installed Harness remains 0.54.1 and its observed shared AGENTS template hash remains `86cc1595619c097f79ef8e4234367e9ba8b056a9635bc24d372bcf417b024aa6`; applicable shared rules are current by meaning, with stricter repository governance retained. Loaded identity is unknown. Document sync is review_required for first baseline, unknown loaded identity and historical README migration pointers; those are not hidden or relabeled PASS. Next: owner can inspect the updated rules; actual faulty HiFi requires its artifact for diagnosis. Publication and all-seven-skill renewal remain pending the paused work's resumption.

### Taste reference review — 2026-09-25

Owner requested learning from Taste Skills. Reviewed relevant local `design-taste-frontend` sections and compared v1/gpt-taste baselines. Added selective adoption to the already-routed modern design source guide and all four README descriptions: brief interpretation, task-fit density, component/layer roles, CTA meaning and font clearance, and preservation during redesign. Kept marketing-only presets out of native/dense product UI; rejected forced motion/assets, arbitrary typography bans, copy truncation, overflow masking and fabricated execution. No dependency, schema, author, approval or runtime change. This is reference analysis, not a full invocation of those generation workflows.

Observed local source identities (not upstream versions or loaded identities):
- `design-taste-frontend/SKILL.md`: sha256 `2e064e92aca020b2e0bad69326fe7ea55d59005ed53d1a8cbce1bd135d44b8b3`.
- `design-taste-frontend-v1/SKILL.md`: sha256 `b7d3715c36c93ce1ac44c60586dd7974a2c2c094aff06c3a72b736314e60d8ac`.
- `gpt-taste/SKILL.md`: sha256 `6a19b70e6b53761d5788f284c615bf93a07cd9f94d5f0a452520260d9d951523`.

Taste follow-up checks: specification, document weight, all 11 UI skill-contract tests, all 15 cross-skill tests and whitespace checks pass. Evidence and source hashes: `C:/Users/mps19/AppData/Local/Temp/pdh-taste-reference-orez03gl`. The earlier 281-test UI run remains prior evidence; this prose-only addition did not repeat the full suite or repair the two broader Harness failures. Document sync retains baseline/loaded-identity and historical-pointer review findings. Repository, branch, HEAD, installed template identity and publication pause are unchanged from the preceding handoff. Guide routing, four README descriptions and this Epic agree; no installed skills or product artifacts were changed.

### Page-specific Taste profiles — 2026-09-25

Owner accepts font guidance, stronger motion and two-line headlines as references, asking for different settings for landing, portfolio and backend pages. Added `page-design-profiles.md`: landing, portfolio, dashboard, data and form settings for typography, headline, density and motion. Routed entry/intake/HiFi/motion to the same existing Design Brief and MM decisions; reconciled the earlier broad rejection in the modern-source guide and four READMEs. No new schema, dependency, gate or product redesign. UI impact none in this source repository.

Acceptance: mixed-profile products keep shared brand but different task settings; expressive motion is a proposal until selected, then actual effect and reduced-motion evidence are required. Desktop hero line targets retain mobile/localization/text-scaling exceptions and approved copy. Page-level font avoid choices do not ban platform or brand fonts globally. Prior accepted no-motion decisions remain valid. Existing overlap/clipping gates and repair limits still apply. Baseline is unchanged on `codex/wireframe-four-widths` at `077cd3471d267d2c2f093c8d06f3f76a293ed1e1`; prior index state, unrelated audit files and design script are preserved. Publication remains paused. Verification pending.

Profile verification: specification, document weight, all 11 UI skill-contract tests, 60 Harness skill-contract tests, 15 cross-skill tests and whitespace checks passed. Logs and exact source hashes: `C:/Users/mps19/AppData/Local/Temp/pdh-page-profiles-_gouoqu_`. No actual product preview is newly validated; the full suite was not repeated for this prose-only change and the two earlier Harness failures remain open. Entry, intake, HiFi, motion guidance and four READMEs agree on page-specific settings and unchanged approval ownership. No new generated artifact class or gitignore change. Document sync retains first-baseline, unknown-loaded-identity and historical-pointer review findings. Current repository/branch/HEAD and prior staged bytes are unchanged; installed Harness/template identity and its current shared-rule audit remain as previously recorded. Commit/push/install stay paused.

- 2026-09-25 release continuation: owner resumed commit, push, merge and skill installation. The imported four-width and page-profile work is included on codex/design-flow-glm; EPIC-design-authoring-flow.md records the regression repairs and current 0.54.2 candidate verification. Original checkout bytes are retained.
