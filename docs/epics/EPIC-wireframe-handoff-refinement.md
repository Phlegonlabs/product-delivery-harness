# Wireframe Handoff Refinement

Status: release preparation authorized; exact-candidate verification pending. Route: direct, one writer and one verification sequence.
Baseline: remote main 59ae7203d6fb2c3848d3b058697dd06c4b581ed1; canonical seven-skill bundle. This source repository has no product PRD; the skills and four READMEs are the current contracts.

Problem: design translation, repeated content/interaction work and skill-update impact leave too much interpretation between PRD, wireframe and HiFi.
Accepted outcome: explicit incremental/full-rebuild modes; translation checkpoint; geometry/control/motion and bilingual reading; four Web and three iPhone recipes; source-aware freshness inspection. iPad only when product scope requires it. Preserve approved product requirements, historical artifacts, human approvals and separate native proof.

Write scope: canonical UI skill sources/tests, applicable Product Definition and Harness routing, four README descriptions and this task record. UI impact: both for the reviewer prototype; no production product surfaces or stack change. Template changes require checker/runtime regression.
Authorization: owner approved the discussed scope and explicitly allowed a new worktree/branch. Worktree: product-delivery-harness-wireframe-handoff-refinement; branch: wireframe-handoff-refinement. No commit, push, installation, delegation, archive, deletion or cleanup grants.
Gitignore: no new artifact class; source recipes/tests/docs tracked, existing __pycache__/ rule handles Python output. Verification logs stay outside the checkout.

Dependencies: use current remote canonical composition/search runtime; loaded installed-skill digest is unknown and not equated with disk. No installation or production design approval is claimed.
Checks: all repository-required spec/lint/document-weight/unit/golden-path suites, focused renderer/contract/freshness/recipe regression, git diff --check and ignore hygiene. No formal security PASS without a fixed committed candidate. No real iPhone/native proof from HTML.
Result: accepted local source changes implemented and checked. No release, installation, native-device verification or fixed-SHA security approval is claimed.

## Implementation Evidence

- Added translation/full-rebuild, reading/iPhone, reference-card and freshness guidance; updated UI, Product Definition and Harness entry points plus all four README descriptions.
- Added seven data recipes using the existing renderer, not seven copied shells. Added paired language copy with independent approval/source records, action appearance controls, compact inline navigation and switchable motion annotations. Existing generic overlays remain the route for modal drawers.
- Added read-only freshness inspection for inventoried file/input hashes, loaded/installed skill identity and transitive dependencies. It never certifies complete inventory or approval and never auto-regenerates designs.
- Focused checks: 82 existing wireframe tests passed before the final bounded additions; 15 new refinement tests pass, including closed-menu QA and iPhone-only targets. Final suite results are recorded below.
- Browser repair family: closed details content initially caused a false boundary failure. The first repair skips only hidden disclosure content, reruns QA on toggle and has a negative test proving open overflowing content still fails. No threshold was weakened.
- Isolated Chromium session checked 390/1200 Web targets, click/Enter/Escape, restored summary focus, responsive expanded navigation, bilingual copy inventory, annotation visibility, and 375/430 named iPhone projections. Observed QA passed and browser errors were empty. These are synthetic local renderer checks, not human approval or native-device evidence.
- Browser fixtures/captures: `C:/Users/mps19/AppData/Local/Temp/wireframe-refinement-preview-6c_v3esi/`. Full suite logs: `C:/Users/mps19/AppData/Local/Temp/wireframe-handoff-checks-fg__66iz/`.
- Ignore hygiene: existing .env and __pycache__ rules apply; new source recipes/tests/docs are not ignored. No new ignore entry or untracking is needed.
- Release/version promotion and installed-skill updates remain outside this local uncommitted change. The original checkout's untracked scripts directory and other worktrees are preserved.

## Final Verification

- Required dependency install, skill specification, pyflakes, docs_weight and both staged/unstaged diff whitespace checks completed successfully. Documentation weight grows by 3,223 words versus v0.48.0; the Harness core remains below its existing 3,600-word limit.
- The full Harness run executed 1,156 tests with 16 platform/opt-in skips. Its initial result was 11 failures and 2 errors: installer tests stopped on the newly untracked recipe/source files, plus two documentation contract assertions. That original failure log is retained, not rewritten as PASS.
- Repair: stage only the seven newly authored files (no commit), retain the existing incremental-precedence sentence with its explicit full-rebuild exception, and route freshness through existing references rather than expanding the core skill. The complete failed groups then passed: 60 skill-contract tests and all 15 installer tests. No installer or test assertion was weakened. No source files changed during the installer rerun.
- Other full suites passed: Product Definition 217; UI Design 204 (including all 15 new tests); Design System Compiler 98 with 3 platform skips; Product Activation 56; SEO Growth 16. The opt-in golden path passed (1 test). Final affected joins also passed: strict authority 16, cross-skill pipeline 14 and hybrid publication 2.
- Browser checks are synthetic Chromium evidence: bilingual language blocks and complete copy inventory, motion annotation visibility, compact menu click/Enter/Escape, focus restoration, open-state retention through annotation redraw, desktop expansion and declared phone canvas widths. The freshness CLI also correctly returns review_required for unknown loaded identity without rewriting files.
- Main remains unchanged with its original untracked scripts directory. New files are staged because installer validation requires a tracked source inventory; other edits remain unstaged. No commit, push, tag, installation, cleanup or formal release action occurred.

Remaining separately authorized work: fixed-candidate review and release preparation (including the version bump if promoting), publication/promotion, and installer transaction only when requested. These are not part of this local work authorization. Native iPhone behavior is validated in a target product's implementation stage.

## Release Preparation — 2026-09-21

Owner instruction: "commit and push merge to main" authorizes this scoped branch publication and main merge. Release 0.49.0 includes the required version pins and four-language history. Mandatory installer update follows publication; preserve backups and all worktrees. No cleanup is authorized. Earlier local-only limitations above describe the prior handoff. Remote main was rechecked at the recorded base. Exact candidate and landing results are retained in external verification logs and the PR to avoid changing the reviewed tree.
