interface:
  display_name: "Design Package Builder"
  short_description: "Create distinctive visual, icon, and motion systems"
  default_prompt: "Use $design-package-builder to turn product docs into a brand-specific design system with a concrete taste statement and anti-slop review that preserves exact wording or bounded display contracts, keeps landing pages KISS, labels per-region style/image/media/motion needs, defaults ordinary regions to open layouts, requires a named purpose for every border or elevated container, avoids nested frames and decorative accent rails, uses evidence-based icon and motion systems where applicable, provides runnable motion demos when requested, and produces a UI package."
