interface:
  display_name: "Full-Stack Harness Core"
  short_description: "Plan, authorize, verify, and integrate delivery work"
  default_prompt: "Use $fullstack-harness-engineering to route this work to the lightest safe direct or PLAN-v5/RUN-v10 delivery path, preserving exact authorization, isolated ownership, exact-SHA review, and the repository's branch rules."
policy:
  allow_implicit_invocation: true
