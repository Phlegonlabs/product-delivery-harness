interface:
  display_name: "Product Design Builder"
  short_description: "Create a frozen design-system contract"
  default_prompt: "Use $product-design-builder with $impeccable and $frontend-design to turn the approved PRD UI surface contract into a design-system pair, ask once for my style and visual references, generate and challenge concept worlds, inspect current public references for exactly three product-specific directions, and extract confirmed Adopt / Adapt / Avoid principles before freezing the design system."
