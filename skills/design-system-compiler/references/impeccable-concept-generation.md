# Retired Impeccable Concept Generation Bridge

Do not use this path for new work. Visual direction and HiFi authoring now belong to `../../ui-design-builder/SKILL.md` and use `frontend-design` as the single design author. Impeccable runs afterward in evaluate mode for critique and audit; it does not generate or refine the direction.

This file remains as a compatibility pointer for older instructions. A request that reaches it returns to `ui-design-builder` without creating design-system files or changing Product Definition.
