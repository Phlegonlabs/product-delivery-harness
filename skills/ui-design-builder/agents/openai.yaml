interface:
  display_name: "UI Design Builder"
  short_description: "Create approved wireframes and HiFi UI"
  default_prompt: "Use $ui-design-builder with the full owner-pinned $frontend-design skill as the actual design author for both Wireframes and directions, connected HiFi, and repairs after Product Definition Approval. Before each authoring stage, verify the ui-design bindings and load both skills in the writer's context. Use assemblers and reviewers only for shell construction and validation. Then review HiFi with $impeccable and decide whether $design-system-compiler is required."
